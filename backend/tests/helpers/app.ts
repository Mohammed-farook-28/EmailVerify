/**
 * App re-export for tests.
 * Keeps test imports clean and provides a single place
 * to add test-specific middleware if needed later.
 */
import { app } from '../../src/app.js';
export { app };
